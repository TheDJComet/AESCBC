#include "AES.hpp"
#include <iomanip>
#include <cstring>
#include <iostream>
using namespace std;
//reminder index = row + 4 * c
//for sboxes row + 15 * c
static const uint8_t sbox[256] = {
	
};

static const uint8_t inv_sbox[256] = {
	
};

static const uint8_t Rcon[11] = {
	0x00, 0x01, 0x02, 0x04, 0x08,
	0x10, 0x20, 0x40, 0x80, 0x1B, 0x36
};

AES128::AES128(const array<uint8_t, 16>& key){
	keyExpansion(key.data());
}

//britzam git clone test

array<uint8_t, 4> AES128::rotWord(array<uint8_t, 4> w) const {
	array <uint8_t, 4> rotatedWord = { w[1],w[2],w[3],w[0] };
	
	return rotatedWord;
}

array<uint8_t, 4> AES128::subWord(array<uint8_t, 4> w) const {
	for (int i = 0; i < 4; i++) {
		w[i] = sbox[w[i]];
	}
	return w;
}

void AES128::keyExpansion(const uint8_t key[16]) {
	array<uint8_t, 4> words[44];

	//build W0 to W3 from original key using FIPS 197 1D Array Indexing
	for (int i = 0; i < 4; ++i) {
		words[i][0] = key[4 * i + 0];
		words[i][1] = key[4 * i + 1];
		words[i][2] = key[4 * i + 2];
		words[i][3] = key[4 * i + 3];
	}

	//build W4 and beyond
	for (int i = 4; i < 44; ++i) {
		array<uint8_t, 4> temp = words[i - 1];

		if (i % 4 == 0) {
			temp = subWord(rotWord(temp));
			temp[0] ^= Rcon[i / 4];
		}
		for (int j = 0; j < 4; ++j) {
			words[i][j] = words[i - 4][j] ^ temp[j];
		}
	}
	//add to round keys
	for (int i = 0; i < 44; ++i) { //44 words
		for (int j = 0; j < 4; ++j) { //4 bytes each
			roundKeys[4 * i + j] = words[i][j];
		}
	}
}

void AES128::addRoundKey(uint8_t state[16], int round) const {
	const uint8_t* rk = &roundKeys[16 * round];
	for (int i = 0; i < 16; ++i) {
		state[i] ^= rk[i];
	}
}

void AES128::substituteBytes(uint8_t state[16]) const {
	for (int i = 0; i < 16; ++i) {
		state[i] = sbox[state[i]];
	}
}

void AES128::inverseSubstituteBytes(uint8_t state[16]) const {
	for(int i = 0; i < 16; i++){
		state[i] = inv_sbox[state[i]];
	}
}

void AES128::shiftRows(uint8_t state[16]) const {
	uint8_t temp[16];
	memcpy(temp, state, 16);

	//row 0 so 0 + 4 * col no shift
	state[0] = temp[0];
	state[4] = temp[4];
	state[8] = temp[8];
	state[12] = temp[12];

	//row 1 so 1 + 4 * col (1 left shift)
	state[1] = temp[5];
	state[5] = temp[9];
	state[9] = temp[13];
	state[13] = temp[1];

	//row 2 so 2 + 4 * col (2 left shifts)
	state[2] = temp[10];
	state[6] = temp[14];
	state[10] = temp[2];
	state[14] = temp[6];

	//row 3 so 3 + 4 * col (3 left shifts)
	state[3] = temp[15];
	state[7] = temp[3];
	state[11] = temp[7];
	state[15] = temp[11];
}

void AES128::inverseShiftRows(uint8_t state[16]) const {
	uint8_t temp[16];
	
	memcpy(temp, state, 16);

	//Row 0 - no shift
	state[0] = temp[0];
	state[4] = temp[4];
	state[8] = temp[8];
	state[12] = temp[12];

	//Row 1 - decryption shift RIGHT 1
	state[1] = temp[13];
	state[5] = temp[1];
	state[9] = temp[5];
	state[13] = temp[9];

	//Row 2 - shift RIGHT 2
	state[2] = temp[10];
	state[6] = temp[14];
	state[10] = temp[2];
	state[14] = temp[6];

	//Row 3 - shift RIGHT 3
	state[3] = temp[7];
	state[7] = temp[11];
	state[11] = temp[15];
	state[15] = temp[3];
}

void AES128::printState(const uint8_t state[16], const std::string& label) const {
	cout << "\n" << label << endl;
	int index = 0;
	for (int row = 0; row < 4; row++) {
		for (int col = 0; col < 4; col++) {
			index = row + (4 * col);
			cout << " " << hex << setw(2) << setfill('0') << (int)state[index] << " ";

		}
		cout << endl;
	}
}
void AES128::printBlock(const uint8_t block[16], const std::string& label) const {
	cout << endl << label << ": ";
	for (int i = 0; i < 16; ++i) {
		cout << hex << setw(2) << setfill('0') << (int)block[i] << " ";
	}
	cout << dec << endl;
}

void AES128::mixColumns(uint8_t state[16]) const {
	for (int c = 0; c < 4; ++c) {
		int col = 4 * c;

		uint8_t s0 = state[col + 0];
		uint8_t s1 = state[col + 1];
		uint8_t s2 = state[col + 2];
		uint8_t s3 = state[col + 3];

		state[col + 0] = mul(s0, 0x02) ^ mul(s1, 0x03) ^ s2 ^ s3;
		state[col + 1] = s0 ^ mul(s1, 0x02) ^ mul(s2, 0x03) ^ s3;
		state[col + 2] = s0 ^ s1 ^ mul(s2, 0x02) ^ mul(s3, 0x03);
		state[col + 3] = mul(s0, 0x03) ^ s1 ^ s2 ^ mul(s3, 0x02);

		
	}
}

void AES128::inverseMixColumns(uint8_t state[16]) const {
	for(int c = 0; c < 4; c++) {
		int col = 4 * c;

		uint8_t s0 = state[col + 0];
		uint8_t s1 = state[col + 1];
		uint8_t s2 = state[col + 2];
		uint8_t s3 = state[col + 3];

		state[col + 0] = 
			mul(s0, 0x0e) ^ mul(s1, 0x0b) ^ mul(s2, 0x0d) ^ mul(s3, 0x09);
		state[col + 1] =
			mul(s0, 0x09) ^ mul(s1, 0x0e) ^ mul(s2, 0x0b) ^ mul(s3, 0x0d);
		state[col + 2] =
			mul(s0, 0x0d) ^ mul(s1, 0x09) ^ mul(s2, 0x0e) ^ mul(s3, 0x0b);
		state[col + 3] = 
			mul(s0, 0x0b) ^ mul(s1, 0x0d) ^ mul(s2, 0x09) ^ mul(s3, 0x0e);
	}
}

void AES128::encryptBlock(uint8_t in[16], uint8_t out[16]) const {
	uint8_t state[16];
	memcpy(state, in, 16);
	printState(state, "Initial State");
	addRoundKey(state, 0);
	printState(state, "After Adding Round 0 key");
	int round = 1;
	for (round; round < 10; ++round) {
		cout << endl << "Round " << round << endl;
		substituteBytes(state);
		printState(state,"After SubBytes");
		shiftRows(state);
		printState(state,"After ShiftRows");
		mixColumns(state);
		printState(state,"After MixColumns");
		addRoundKey(state, round);
	}
	substituteBytes(state);
	printState(state,"After Final SubBytes");
	shiftRows(state);
	printState(state, "After Final ShiftRows");
	addRoundKey(state, round);
	printState(state, "After Final Round Key Addition");

	memcpy(out, state, 16);
}

void AES128::decryptBlock(uint8_t in[16], uint8_t out[16]) const {
	uint8_t state[16];

	memcpy(state, in, 16);

	printState(state, "Initial Ciphertext State: ");

	//Initial AddRoundKey w/ last round key (10)
	addRoundKey(state, 10);
	printState(state, "After initial addRoundKey (10)");

	//Remaining Rounds
	for(int i = 9; i >= 1; i--){
		cout << endl << "Inverse Round #" << i << endl;

		inverseShiftRows(state);
		printState(state, "After inverseShiftRows: ");

		inverseSubstituteBytes(state);
		printState(state, "After inverseSubstituteBytes: ");

		addRoundKey(state, i);
		printState(state, "After AddRoundKey: ");

		inverseMixColumns(state);
		printState(state, "After inverseMixColums: ");
	}

	//Round 0 - follows encryption pattern
	inverseShiftRows(state);
	printState(state, "After final inverseShiftRows: ");

	inverseSubstituteBytes(state);
	printState(state, "After final inverseSubstituteBytes: ");

	addRoundKey(state, 0);
	printState(state, "After final addRoundKey (Round 0): ");

	memcpy(out, state, 16);
}

uint8_t AES128::xtime(uint8_t x) {
	uint8_t bitOver = x & 0x80; //checks if MSB is set or not, if given bit is set the & will overflow into 8-th bit

	x <<= 1; //1 left shift

	if (bitOver) {
		x ^= 0x1b; //if overflow occured reduced given byte by mod
	}

	return x;
}

uint8_t AES128::mul(uint8_t x, uint8_t by) {
	uint8_t result = 0;

	while (by) {			//loop through the bits of given by
		if (by & 1) {		//if the current bit (initially LSB) is 1
			result ^= x;	//XOR the result with the current x
		}
		x = xtime(x);		//shift and reduced the x (when thinking of polynomials usually its larger by smaller hence why we reduce the x and not vice verse)
		by >>= 1;			//shift by right 1
	}
	return result;
}


